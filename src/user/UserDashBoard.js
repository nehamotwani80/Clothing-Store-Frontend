import React from 'react';
import Base from '../core/Base';

const UserDashBoard = () => {
	return (
		<Base title="User Dash-board">
			<p>this is UserDashBoard route</p>
		</Base>
	);
};

export default UserDashBoard;
