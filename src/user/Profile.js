import React from 'react';
import Base from '../core/Base';

const Profile = () => {
	return (
		<Base title="Profile">
			<p>this is profile route</p>
		</Base>
	);
};

export default Profile;
